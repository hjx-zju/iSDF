# 基于神经隐式表示的轻量化建图交接说明

更新时间: 2026-03-16
当前工作分支: `entropy_isdf`
github地址：https://github.com/hjx-zju/iSDF
## 1. 项目概述

本项目基于 iSDF (Real-time Neural Signed Distance Fields) 实现在线/离线 SDF 重建，支持三类使用模式，具体可参考 README.md:

1. 离线序列训练与评估 (ReplicaCAD / ScanNet)。
2. ROS + Realsense + ORB-SLAM3 在线建图。
3. ROS + Franka + Realsense 在线或离线建图。

当前分支 `entropy_isdf` 在原始 iSDF 基础上增加了熵编码相关能力，主要用于模型压缩率/率失真权衡实验，对应毕业论文的研究内容一和专利。


## 2. 环境与依赖

## 2.1 Python 环境

推荐使用 conda，Python 版本为 `3.8.x`。

- 基线环境文件: `environment.yml`
- 我的本地实验环境: `environment_my.yml`

创建环境示例:

```bash
conda env create -f environment_my.yml
conda activate isdf
pip install -e .
```

说明:

- `environment_my.yml` 包含额外熵编码压缩相关依赖，如 `compressai`、`tensorflow-compression`。
- PyTorch 在 `environment_my.yml` 中固定为 `torch==1.13.1+cu117`，部署机器需匹配 CUDA。

## 2.2 ROS/SLAM 依赖

在线建图依赖 ROS Noetic 与 ORB-SLAM3:

- ORB-SLAM3 wrapper: `ORB_SLAM3_ros/`
- iSDF ROS launch: `isdf/launch/train.launch`、`isdf/launch/train_franka.launch`

若使用 Realsense 在线模式，需确保:

- 已安装 `realsense2_camera` ROS 驱动。
- ORB-SLAM3 的 `libORB_SLAM3.so` 可被 `ORB_SLAM3_ros` 正确链接。

## 2.3 数据集下载
参见 README.md，也可在我主机的路径`/home/hjx/dataset/iSDF/`下找到
## 3. 一键上手路径 

## 3.1 最小离线跑通

```bash
conda activate isdf
cd isdf/train
python train_vis.py --config configs/replicaCAD.json
```

输出默认写入:

- `results/iSDF/<时间戳>/`

常见结果:

- `config.json`：实验参数配置
- `res.json`：实验结果指标
- `vox_res.json`：实验结果指标
- `slices/`：切片结果可视化
- `meshes/`：mesh结果可视化

## 4.2 熵编码实验跑通

`train.py` 新增了 `-et/--use_entropy` 参数:

```bash
conda activate isdf
cd isdf/train
python train.py --config configs/replicaCAD.json -et
```

说明:

- `-hd` 表示 headless 模式，
- 启用 `-et` 后，使用熵编码压缩，结果目录名会自动追加 `_entropy` 后缀。
- 率失真权重在json文件中的`'lambda_RD'`设置。


## 5. 代码结构与职责

核心 Python 目录: `isdf/`

- `isdf/train/`: 训练入口、批量实验脚本、配置。
- `isdf/modules/`: 模型与训练核心逻辑。
- `isdf/datasets/`: 数据读取、相机模型、预处理。
- `isdf/eval/`: 指标与评估工具。
- `isdf/visualisation/`: 2D/3D 可视化与窗口交互。
- `isdf/launch/`: ROS 启动文件。

关键入口文件:

- `isdf/train/train.py`: 主训练流程，支持 `--use_entropy`。
- `isdf/train/train_vis.py`: Open3D GUI 可视化训练。
- `isdf/train/batch_train/jobs_local.py`: 批量实验入口。
- `isdf/modules/trainer.py`: 训练器主逻辑与 eval/save。
- `isdf/modules/fc_map.py`: SDF 网络与 EntropySDFMap。

## 6. 本分支定制点 

## 6.1 熵编码相关改动

主要改动路径:

- `isdf/modules/smcper/` (新增压缩相关模块)
- `isdf/modules/fc_map.py` (引入 `EntropyLinear` / `EntropySDFMap`)
- `isdf/modules/trainer.py` (训练时增加 rate loss)
- `isdf/train/train.py` (新增 `-et` 参数与压缩结果记录)

行为变化:

- 启用 `--use_entropy` 时，`total_loss` 增加 `lambda_RD * rate`。
- 训练中会打印压缩大小/压缩率信息。
- 保存结果中会附加压缩相关字段。

## 7. 数据与结果约定

数据目录:

- `data/seqs/` 序列数据
- `data/gt_sdfs/` GT SDF 与 mesh
- `data/eval_pts/` 评估点集

结果目录:

- `results/iSDF/` 每次训练自动按时间戳落盘
- `results/figs/` 图表类结果

批量实验默认配置来源:

- `isdf/train/batch_train/jobs_local.py`
